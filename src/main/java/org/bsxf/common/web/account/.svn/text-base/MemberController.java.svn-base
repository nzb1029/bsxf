package org.bsxf.common.web.account;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;

/**
 * @类名称：MemberController   
 * @类描述：   
 * @作者：Administrator   
 * @创建时间：2014-10-24 下午01:33:13   
 * @修改人：Administrator   
 * @修改时间：2014-10-24 下午01:33:13   
 * @修改备注：   
 * @版权：
 * @version
*/
@Controller
@RequestMapping(value = "/member/member")
public class MemberController {/*
	@Autowired
	private MemberManager memberManager;

	@RequestMapping(value = { "list" })
	public String list(Model model) {

		return "member/memberList";
	}

	@RequestMapping(value = { "jqgrid" })
	@ResponseBody
	public Map jqgrid(HttpServletRequest request) {
		Page page = JqGirds.parmsToPage(request);
		page = memberManager.findMemberByPage(page);
		return JqGirds.renderJson(page);
	}

	@RequestMapping(value = { "create" })
	public String create(HttpServletRequest request, Model model) {
		String id = request.getParameter("id");
		Member member = new Member();
		if (StringUtils.isNotBlank(id)) {
			member = memberManager.getMember(id);
		}
		model.addAttribute("member", member);

		return "member/memberForm";
	}

	@RequestMapping(value = { "save" })
	public String save(Member member, RedirectAttributes redirectAttributes, HttpServletRequest request) {

		memberManager.saveOrUpdateMember(member);
		redirectAttributes.addFlashAttribute("message", "创建菜单" + member.getName() + "成功");
		return "redirect:/member/member/list";
	}

	@SuppressWarnings("unchecked")
	@RequestMapping(value = "delete/{id}")
	@ResponseBody
	public String delete(@PathVariable("id")
	String id) {
		memberManager.deleteMember(Collections3.extractToList(id.split(",")));
		return "true";
	}

*/
}
